[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "greenhouse-emission-predictor"
version = "0.0.1"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
